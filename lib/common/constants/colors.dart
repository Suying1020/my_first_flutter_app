import 'package:flutter/material.dart';

class MyAppColors{
  MyAppColors._();

  static const Color white = Colors.white;
  static Color error = Colors.red.shade700;
  static const Color primaryColor = Color.fromRGBO(66, 133, 244, 1);

  // textFormField colors
  static const textFieldEnabled = Color.fromRGBO(218, 218, 218, 1);
  static const hintText = Color.fromRGBO(128, 128, 128, 1);
}